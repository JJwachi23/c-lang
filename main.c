// Libraries
#include<stdio.h> // stdio = standard input output
#include<stdbool.h> // stdbool = standard boolean

// Datatypes
// int = 1234, -1234
// char (charactor) = "", '' ("Hello C") 
// bool (boolean) = validate value (true, false)
// float = 3.14 (decimal point)

// double = 3.145523 (more than 2 decimal pint)
// NULL = (Empty value)

// Variable name
// 1. Datatypes 2. Variable name 3. Value default
// int x = 10;
// char fullName = 'EDCO';
// isYoung = flase;

// Conditional
// 1. if (if the conditional is TRUE)
// 2. if...else (if gonna chose the conditional just one)
// 3. if...else if (if want to check conditional more than 1)
// 4. if...else if...else ()

// function main() it's means the initial first of all the program in C
int main() {
    bool isAdult = true;
    
    // printf() it's means command to print show message or data
    printf("Hi, C");

    // * return for end the function
    return 0;
}

// Git version control

// git add . = Added file (All of file change)

// Add:
// Edit:
// Deleted: